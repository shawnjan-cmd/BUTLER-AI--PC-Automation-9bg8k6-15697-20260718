/**
 * index.js — PATCHED by tools/postinstall.js
 * Delegates to stubs/hermes-parser-plugin.js (git-tracked, never wiped).
 */
'use strict';
const path = require('path');
const fs   = require('fs');
function findRoot(startDir) {
  let dir = path.dirname(startDir);
  for (let i = 0; i < 14; i++) {
    if (fs.existsSync(path.join(dir, 'metro.config.js'))) return dir;
    const parent = path.dirname(dir);
    if (parent === dir) break;
    dir = parent;
  }
  return startDir;
}
const projectRoot = findRoot(__dirname);
module.exports = require(path.join(projectRoot, 'stubs', 'hermes-parser-plugin.js'));
