# Archived unsafe runtime-code compiler

This inactive legacy component used `new Function(...)` to compile widget code at runtime. It is intentionally excluded from the active Expo source tree and retained only for forensic reference.
